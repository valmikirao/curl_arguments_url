jq>=1.1.2<2.0.0
PyYAML>=6.0.0,<7.0.0

requests>=2.28.2,<3.0.0

# not liking swagger parser currently
swagger-parser>=1.0.2,<2.0.0
six>=1.16.0,<2.0.0  # needed for swagger-parser but not install with it for some reason

